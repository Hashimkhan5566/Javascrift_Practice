let color = "red";

if(color === "red")
    {
    console.log(`welcome to ${color} to stop the car!`);
}

if(color === "yellow")
    {

    console.log(`welcome to ${color} to slow move the car`);
}

if(color === "green")
    {
    console.log(`welcome to ${color} to gove move the car`);
}